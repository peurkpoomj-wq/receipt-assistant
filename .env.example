# ============================================================
# LINE Messaging API
# ได้จาก: LINE Developers Console → Your Channel → Basic settings
# ============================================================
LINE_CHANNEL_SECRET=your_line_channel_secret_here
LINE_CHANNEL_ACCESS_TOKEN=your_line_channel_access_token_here

# ============================================================
# OpenAI API (GPT-4o Vision)
# ได้จาก: platform.openai.com/api-keys
# ============================================================
OPENAI_API_KEY=sk-...your_openai_api_key_here
OPENAI_MODEL=gpt-4o

# ============================================================
# Google Sheets API (Service Account)
# วิธีตั้งค่า:
#   1. Google Cloud Console → Create Service Account
#   2. Download JSON key → วางไว้ที่ credentials/google-service-account.json
#   3. Share your Google Sheet กับ service account email (Editor)
# ============================================================
GOOGLE_SERVICE_ACCOUNT_KEY_PATH=./credentials/google-service-account.json
# หรือจะใส่ JSON ทั้งก้อนใน env var นี้แทน (สำหรับ deploy บน cloud):
# GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}

GOOGLE_SPREADSHEET_ID=your_spreadsheet_id_from_url_here
GOOGLE_SHEET_NAME=Expenses

# ============================================================
# รายงานอัตโนมัติ (Daily Flash Report)
# ============================================================
# LINE User ID ของ admin ที่จะรับรายงาน (คั่นด้วย comma)
ADMIN_LINE_USER_IDS=Uxxxxxxxxx,Uyyyyyyyyy

# หรือ LINE Group ID ที่ต้องการรับรายงาน
# REPORT_GROUP_ID=Cxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# เวลาส่งรายงานทุกวัน (จันทร์–ศุกร์) ค่าเริ่มต้น 18:00
DAILY_REPORT_TIME=18:00
REPORT_TIMEZONE=Asia/Bangkok

# ============================================================
# Rich Menu (ตั้งค่าถ้าต้องการใส่ภาพเมนู)
# ต้องเป็นไฟล์ PNG ขนาด 2500x843 pixel
# ============================================================
# RICHMENU_IMAGE_PATH=./assets/richmenu.png

# ============================================================
# App Config
# ============================================================
PORT=3000
NODE_ENV=development
